name: layrz_logging
description: "Logging system for Flutter applications"
version: "1.6.0"
homepage: https://github.com/goldenm-software/layrz_logging

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter: { sdk: flutter }

dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^6.0.0

platforms:
  android:
  ios:
  web:
  windows:
  linux:
  macos:

flutter: